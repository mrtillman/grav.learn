Write a program that saves a base64 encoded image.
HINT: you will need to use the Node.js File System Module. 
      see the spoiler code if you get stuck. good luck.

spoiler code:
https://github.com/mrtillman/grav.client/blob/master/examples/grav.saveEncodedImage.js

Here is some boilerplate code to get you started:

  const { GravatarClient } = require('grav.client');
  const fs = require('fs');

  module.exports = function () {
    const image = "/path/to/file.png";
    const client = new GravatarClient("user@example.com", "password");
    // ...
  }

Use `$ADVENTURE_COMMAND verify PROGRAM.js` to verify your program.
