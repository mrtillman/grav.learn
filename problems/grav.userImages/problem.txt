Write a program that gets all gravatar images in the account.

Here is some boilerplate code to get you started:

  const { GravatarClient } = require('grav.client');

  module.exports = function () {
    const client = new GravatarClient("user@example.com", "password");
    // ...
  }

Use `$ADVENTURE_COMMAND verify PROGRAM.js` to verify your program.
